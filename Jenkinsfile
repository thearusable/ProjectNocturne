#!groovy

pipeline {
    agent any
    
    stages{
        steps ('checkout'){
            checkout scm
        }
    
        steps ('build'){
            //build
        }
    
        steps ('test'){
            //test
        }
    }
}
